@model DEMO_PuellaSchoolAPP.Models.GradeModel

@{
    ViewData["Title"] = "Edición de Registro de Grado";
}

<h1 style="text-align:center">Edición de Registro de Grado</h1>

<hr />

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <form asp-action="Edit" onsubmit="return confirmAction();">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="mb-3">
                    <input asp-for="GradeId" type="hidden" class="form-control" />
                </div>
                <div class="form-group">
                    <label asp-for="GradeName" class="control-label">Nombre del Grado</label>
                    <input asp-for="GradeName" class="form-control" />
                    <span asp-validation-for="GradeName" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <input type="submit" value="Editar" class="btn btn-primary" />
                    <a asp-action="Index" class="btn btn-danger">Regresar al listado</a>
                </div>
            </form>
        </div>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
